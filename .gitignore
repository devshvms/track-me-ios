# Xcode
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.xcuserstate

# Swift Package Manager
.build/

# CocoaPods
Pods/

# Carthage
Carthage/Build/

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output

# OS X
.DS_Store

# Firebase config. Local-only and never committed; CI restores it from the
# GOOGLE_SERVICE_INFO_PLIST_BASE64 secret. It must sit in track-me-ios/ because
# FirebaseApp.configure() reads it out of the app bundle at runtime.
track-me-ios/GoogleService-Info.plist

# Signing and API credentials — never commit these.
# The App Store Connect key is issued once and cannot be re-downloaded; the
# certificate and provisioning profiles are account-bound. CI reads all of them
# from GitHub Actions secrets, so nothing here needs to be in the repo.
*.p8
*.p12
*.cer
*.certSigningRequest
*.mobileprovision
